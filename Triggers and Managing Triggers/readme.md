## Triggers and Managing Triggers
A trigger in SQL is a special stored procedure that runs automatically when an INSERT, UPDATE or 
DELETE operation occurs on a table. It helps automate actions and keep data consistent.

Managing triggers involves viewing existing triggers, 
modifying them (which usually requires dropping and recreating), and removing them when they are no longer needed.
